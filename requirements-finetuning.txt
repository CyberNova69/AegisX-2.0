# Fine-Tuning GPU Dependencies (v0.1)
# =====================================
# Install ONLY on the college GPU PC, NOT on the dev laptop.
#
# These packages require an NVIDIA CUDA GPU:
#   pip install -r requirements-finetuning.txt
#
# For the base project (no fine-tuning), use requirements.txt instead.

torch>=2.1.0
transformers>=4.40.0
datasets>=2.19.0
peft>=0.10.0
trl>=0.8.0
accelerate>=0.28.0
bitsandbytes>=0.43.0
